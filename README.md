# Raid

Terminal coding agent (Rust + Ratatui).

## Requirements

- Rust nightly (see `rust-toolchain.toml`)

## Run

```bash
cargo run
```

Type into the composer, press `Enter` to submit, or press `Esc` to quit.
